import { styled } from 'styled-components'

export const StyledFooter = styled.footer`
    display: flex;
    flex-direction: row;
    justify-content: flex-end;
    padding: 20px 10px;
    position: absolute;
    bottom: 0;
    right: 0;
`